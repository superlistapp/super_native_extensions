name: super_data_transfer
description: Low level interface to clipboard and DnD
version: 0.0.1
publish_to: none
homepage:

environment:
  sdk: ">=2.17.0-239.0.dev <3.0.0"
  flutter: ">=2.5.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_web_plugins:
    sdk: flutter
  meta: ^1.7.0
  nativeshell_core:
    git:
      url: https://github.com/nativeshell/nativeshell_ng.git
      ref: 3fa95c74a4cfaafc9631e8c1802f9ecb10531d88
      path: core/dart

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:
  # This section identifies this Flutter project as a plugin project.
  # The 'pluginClass' specifies the class (in Java, Kotlin, Swift, Objective-C, etc.)
  # which should be registered in the plugin registry. This is required for
  # using method channels.
  # The Android 'package' specifies package in which the registered class is.
  # This is required for using method channels on Android.
  # The 'ffiPlugin' specifies that native code should be built and bundled.
  # This is required for using `dart:ffi`.
  # All these are used by the tooling to maintain consistency when
  # adding or updating assets for this project.
  plugin:
    platforms:
      android:
        package: com.superlist.super_data_transfer
        pluginClass: SuperDataTransferPlugin
      ios:
        pluginClass: SuperDataTransferPlugin
      linux:
        pluginClass: SuperDataTransferPlugin
      macos:
        pluginClass: SuperDataTransferPlugin
      windows:
        pluginClass: SuperDataTransferPluginCApi
      web:
        pluginClass: SuperDataTransferWeb
        fileName: super_data_transfer_web.dart
